# XYZ AI project instructions

Build this project in the supplied 11 phases, one prompt per phase. Work only on the currently requested phase, verify that phase runs before proceeding, and do not build ahead. Keep LLM API keys only in environment variables (for example, `.env`), never hardcoded.
